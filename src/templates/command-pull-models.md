拉取模型列表（model-hub）。

执行下面的命令并列出已配置的自定义供应商：

```
{{CLI_PATH}} sync --list
```

然后询问用户要同步哪个供应商；用户指明后执行：

```
{{CLI_PATH}} sync --provider <供应商id或名称>
```

用户说"全部"时改用 `sync --all`。最后告知：打开 ZCode 模型选择器即可看到新模型；
同步失败的供应商附上 CLI 的错误输出（已脱敏）。
