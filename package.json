{
  "name": "zcode-suite",
  "version": "1.0.0",
  "description": "Unified ZCode desktop plugin: model-hub model pulling + zcode+ prompt enhancement — one install, one backup chain, one repair trigger",
  "type": "module",
  "license": "MIT",
  "bin": {
    "zcode-suite": "bin/zcode-suite.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/",
    "check": "node scripts/check-syntax.mjs",
    "verify": "node scripts/verify-smart-config.mjs",
    "build": "node scripts/build.mjs"
  }
}
